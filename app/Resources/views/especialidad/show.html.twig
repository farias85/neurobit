{% extends ':backend:backend.layout.html.html.twig' %}
{% block title %}Datos de Especialidad {% endblock %}
{% block nombre %}Equipo{% endblock %}
{% block subtitulo %}Datos de Especialidad {% endblock %}
{% block content %}
    <table class="table table-responsive">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ especialidad.id }}</td>
        </tr>
        <tr>
            <th>Nombre</th>
            <td>{{ especialidad.nombre }}</td>
        </tr>
        <tr>
            <th>Descripción</th>
            <td>{{ especialidad.descripcion }}</td>
        </tr>
        </tbody>
    </table>

    <br>
    <a href="{{ path('especialidad_index') }}" class="btn btn-success">Volver a la lista</a>

    <br><br>
    <a href="{{ path('especialidad_edit', { 'id': equipo.id }) }}" class="btn btn-info">Editar</a>

    <br><br>
    {{ form_start(delete_form) }}
    <input type="submit" value="Eliminar" class="btn btn-danger">
    {{ form_end(delete_form) }}

{% endblock %}
