{% extends 'base.html.twig' %}

{% block body %}
    <h1>Lista de Especialidades</h1>

    <table>
        <thead>
        <tr>
            <th>Id</th>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Acciones</th>
        </tr>
        </thead>
        <tbody>
        {% for especialidad in especialidads %}
            <tr>
                <td><a href="{{ path('especialidad_show', { 'id': especialidad.id }) }}">{{ especialidad.id }}</a></td>
                <td>{{ especialidad.nombre }}</td>
                <td>{{ especialidad.descripcion }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('especialidad_show', { 'id': especialidad.id }) }}">Mostrar</a>
                        </li>
                        <li>
                            <a href="{{ path('especialidad_edit', { 'id': especialidad.id }) }}">Editar</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('especialidad_new') }}">Crear nueva especialidad</a>
        </li>
    </ul>
{% endblock %}
